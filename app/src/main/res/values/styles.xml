<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TabView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_weight">1</item>
    </style>

    <style name="TabViewText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_weight">1</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">@dimen/tab_view_text_size</item>
        <item name="android:textColor">@color/tab_text_selector</item>
        <item name="android:textStyle">normal</item>
    </style>

    <style name="TabViewImage">
        <item name="android:layout_width">@dimen/tab_img_size</item>
        <item name="android:layout_height">@dimen/tab_img_size</item>
        <item name="android:layout_margin">@dimen/tab_img_margin</item>
    </style>
</resources>
